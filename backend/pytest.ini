[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
# 整个会话共用一个事件循环: 模块级单例(async_engine/检查点池)绑定循环,
# 每个测试换循环会导致 "attached to a different loop" 错误
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
